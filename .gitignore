

# Python virtual env
.venv/

# Скомпилированные артефакты
*.o
*.so
test_freetype
test_gost

# VS Code
.vscode/

# Copilot instructions (локальные)
.github/

# Python кэш
__pycache__/
*.pyc

# Временные файлы сборки
*.tmp

# ── KiCad исходники (большие, скачиваются скриптом) ──
/kicad-src/

# ── Кэш собранных артефактов ──
# Хранится в cache/, но .kiface.meta оставляем для информации
cache/kicad-*/
# Старый формат кэша (один файл) — оставляем
!cache/_eeschema.kiface.*

# ── Бэкап оригинальных системных файлов ──
cache/*-original/

